// given the map as input, find the optimal path from given robots position to a given node
// The type of pathfind algorithm is subject to change

/* TODO: 
    initialise:
        set the nodes, walls, path etc. Everything a star needs to compute the path    
    calcPath:
        parameters: robots_pos and end_point
        return: list of vectors containing the optimal path nodes
        OR      boolean indicating if a path is found or not, or the destination is reached
        given the set of nodes and boundaries, calculate the optimal path between the robots_pos and end_point
*/